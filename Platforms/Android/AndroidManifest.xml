<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.WAKE_LOCK" />
	
	<application
		android:allowBackup="true"
		android:icon="@mipmap/appicon"
		android:roundIcon="@mipmap/appicon_round"
		android:supportsRtl="true"
		android:enableOnBackInvokedCallback="true"
		android:largeHeap="true">
		
		<!-- Comprehensive Firebase Crashlytics disabling -->
		<meta-data
			android:name="firebase_crashlytics_collection_enabled"
			android:value="false" />
		
		<meta-data
			android:name="firebase_crashlytics_debug_enabled"
			android:value="false" />
		
		<meta-data
			android:name="firebase_analytics_collection_deactivated"
			android:value="true" />
		
		<meta-data
			android:name="google_analytics_deactivated"
			android:value="true" />
		
		<!-- Remove Crashlytics API key if present -->
		<meta-data
			android:name="google_crashlytics_api_key"
			tools:node="remove" />
		
		<!-- Disable Firebase InitProvider to prevent auto-initialization -->
		<provider
			android:name="com.google.firebase.provider.FirebaseInitProvider"
			android:authorities="${applicationId}.firebaseinitprovider"
			android:enabled="false"
			android:exported="false" />
		
		<!-- Disable Crashlytics InitProvider -->
		<service
			android:name="com.google.firebase.crashlytics.CrashlyticsInitProvider"
			android:enabled="false"
			android:exported="false"
			tools:node="remove" />
		
		<!-- Disable Crashlytics service -->
		<service
			android:name="com.google.firebase.crashlytics.internal.service.FirebaseCrashlyticsService"
			android:enabled="false"
			android:exported="false"
			tools:node="remove" />
		
		<!-- Google Sign-In Configuration -->
		<activity android:name="com.google.android.gms.auth.api.signin.internal.SignInHubActivity"
			android:theme="@android:style/Theme.Translucent.NoTitleBar"
			android:exported="false" />
	</application>
</manifest>
